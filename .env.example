# Copy to .env.local (gitignored) for local dev against a local backend.
# VITE_* vars are inlined into the built bundle at build time — visible to
# anyone who views source, so only ever put base URLs here, never secrets.
VITE_AUTH_SERVICE_URL=http://localhost:8081
